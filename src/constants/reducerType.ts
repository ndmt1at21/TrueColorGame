const CHANGE_GAME_STATUS = 'CHANGE_GAME_STATUS';
const NEXT_QUIZ = 'NEXT_QUIZ';
const ADD_SCORE = 'ADD_SCORE';
const RESET = 'RESET';
const PERSIST = 'PERSIST';
const ANSWER = 'ANSWER';
const CHANGE_QUIZ_STATUS = 'CHANGE_QUIZ_STATUS';

const reducerType = {
	CHANGE_GAME_STATUS,
	NEXT_QUIZ,
	ADD_SCORE,
	RESET,
	PERSIST,
	ANSWER,
	CHANGE_QUIZ_STATUS
};

export default reducerType;
